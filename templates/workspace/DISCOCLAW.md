# DISCOCLAW.md - Legacy Workspace Stub

This workspace file is no longer the source of default system instructions.

Discoclaw injects tracked defaults from a repository-managed runtime layer.
Use `workspace/AGENTS.md` for your personal rules and overrides.

## Runtime Instruction Precedence

1. **Immutable security policy**
2. **Tracked runtime-injected defaults**
3. **`workspace/AGENTS.md` overrides**
4. **Memory and other runtime context**

If you have old instructions here, move them to `workspace/AGENTS.md`.
